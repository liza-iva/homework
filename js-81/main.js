// const numbers = [1, 2, 3, 4, 5]

// for (let i = 0; i < numbers.length; i++) {
//     console.log(numbers[i] + 1)
// }

// -----------------------DZ-----------------------

// -----------------------1-----------------------

const numbers = [2, 6, 1, 4, 7]

for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] % 2 === 0) {
        console.log(numbers[i])
    }
}

// -----------------------2-----------------------

const color = ['фиолетовый', 'голубой', 'синий', 'зеленый', 'желтый', 'оранжевый', 'красный']

for (let y = 6; y >= 0; y--) {
    console.log(color[y])
}